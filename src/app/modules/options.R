options("shiny.custom.semantic.cdn" = "semantic")

baseSize <- 300
thumbnailSize <- round(baseSize/10)

icons <- list(
  "Stars" = "star",
  "Clouds" = "cloud",
  "Cats" = "cat",
  "Circles" = "circle",
  "Fishes" = "fish",
  "Apples" = "fruit-apple"
)

loaders <- list(
  "Double line" = "double",
  "Single line" = "single"
)
